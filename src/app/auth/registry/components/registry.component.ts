//Wrapper 
/**
 * Component responsable to redirect to register a user in the system
 * Child route
 */
import { Component } from '@angular/core';

@Component({
    template: '<router-outlet></router-outlet>'
})

export class RegistryComponent { }
